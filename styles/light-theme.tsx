import React from "react";
import {createTheme} from "@nextui-org/react";

export const lightTheme = createTheme({
    type: "light",
    theme: {
        colors: {
            background: "#F7F6F4"
        }
    }
})